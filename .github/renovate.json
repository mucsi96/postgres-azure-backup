{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "dependencyDashboard": true,
  "groupName": "all dependencies",
  "groupSlug": "all",
  "packageRules": [
    {
      "groupName": "all dependencies",
      "groupSlug": "all",
      "matchPackagePatterns": ["*"]
    }
  ],
  "labels": ["dependencies"],
  "github-actions": {
    "enabled": false  // Disable GitHub Actions updates as it requires workflow permissions which are not available for pipeline tokens
  }
}
